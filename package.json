{
  "name": "vrm-to-shogun",
  "private": true,
  "version": "0.4.2",
  "type": "module",
  "description": "Browser-based VRM → Vicon Shogun FBX converter. Runs entirely client-side.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "smoke": "node --experimental-strip-types scripts/fbxSmoke.ts && node --experimental-strip-types scripts/stripTest.ts"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
